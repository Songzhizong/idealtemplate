# Ideal Template

**专为 AI 辅助开发优化的 React 19 + TypeScript 高性能全栈开发模板。**

本模板旨在提供一个现代化、类型安全、架构清晰的前端工程基础。采用特性驱动架构（Feature-Based），深度集成 Zod 类型系统，并针对 AI Agent（如 Cline、Windsurf 等）的代码理解与生成进行了特别优化。

项目预置了完整的工程化配置，features 目录下的功能仅作为架构模式的演示示例，可随时替换为你自己的业务逻辑。

## ✨ 核心亮点

- **🧠 AI Native 友好** - 严格的类型约束与代码规范（无 `any`），显著提升 AI 代码生成的准确率与可维护性。
- **🛡️ 极致类型安全** - 以 Zod Schema 为单一数据源（Single Source of Truth），自动推导 TypeScript 类型，实现从 API 响应到 UI 组件的端到端类型安全。
- **⚡️ 现代技术栈** - 基于 **React 19**、**Vite** 和 **Tailwind CSS 4** 构建，拥抱最新生态。
- **🏗️ 特性驱动架构** - 采用 Feature-Based 目录结构，业务逻辑高内聚，便于扩展与维护。
- **🛠️ 顶级工程化** - 集成 Biome (Lint/Format)、Vitest (Unit Test) 和 MSW (API Mock)，开发体验一流。
- **🎨 精美 UI 系统** - 此模板集成了 Shadcn/ui，配合定制化的 Tailwind 配置，开箱即用。

## 📦 技术栈概览

### 核心框架

- **React 19** - 拥抱最新特性（Use API, Ref as Prop 等）
- **TypeScript 5+** - 开启 `strictest` 模式
- **Vite** - 极速冷启动与 HMR
- **Pnpm** - 推荐的包管理器

### 路由与状态

- **TanStack Router** - 强大的文件路由系统，提供极致的类型安全路由体验
- **TanStack Query v5** - 服务端状态管理标准
- **Zustand** - 轻量级全局客户端状态管理
- **Nuqs** - 类型安全的 URL 搜索参数管理

### 数据流与验证

- **Ky** - 轻量且强大的 HTTP 客户端
- **Zod** - 运行时 Schema 声明与校验

### UI 与 样式

- **Tailwind CSS 4.0** - 下一代原子化 CSS 引擎
- **Shadcn/ui** - 高可定制的组件系统
- **Lucide React** - 优雅的图标库
- **Motion** - 声明式动画库 (Framer Motion 精神续作)

## 🚀 快速开始

### 前置要求

- **Node.js**: >= 20.0.0
- **Pnpm**: >= 9.0.0 (本项目强制使用 pnpm)

### 安装与启动

1.  **克隆项目**

    ```bash
    git clone <your-repo-url>
    cd idealtemplate
    ```

2.  **启用 Corepack (可选但推荐)**

    ```bash
    corepack enable
    corepack prepare pnpm@latest --activate
    ```

3.  **安装依赖**

    ```bash
    pnpm install
    ```

4.  **初始化环境**
    - 复制环境变量文件：
      ```bash
      cp .env.dev .env
      ```
    - 初始化 MSW (API Mock)：
      ```bash
      pnpm msw:init
      ```

5.  **启动开发服务器**
    ```bash
    pnpm dev
    ```
    访问 http://localhost:5173 即可看到应用。

## 📂 目录结构

项目采用清晰的分层架构，核心逻辑位于 `src` 目录下：

```text
src/
├── app/                  # 应用级全局配置 (Provider, Route Root, Theme)
├── features/             # 🧩 业务特性模块 (核心)
│   └── {feature-name}/   #按业务领域划分 (e.g., auth, dashboard)
│       ├── api/          # 接口定义 (Zod Schema + Ky Hooks)
│       ├── components/   # 业务专用组件
│       └── ...
├── components/           # 🧩 通用组件库
│   ├── ui/               # Shadcn 基础组件
│   └── common/           # 项目级公共组件
├── lib/                  # 基础设施 (API Client, Utils)
├── hooks/                # 全局 Hooks
├── routes/               # TanStack Router 路由定义
├── mocks/                # MSW Mock 数据定义
└── types/                # 全局类型定义
```

## 🛠️ 开发规范

为了保持代码质量并利于 AI 协作，请遵守以下核心规范（详见 `docs/rules/general_rule.md`）：

1.  **包管理器**: 仅允许使用 `pnpm`。
2.  **类型规范**: 严禁使用 `any`，必须定义 Zod Schema 并推导出类型。
3.  **样式规范**: 优先使用 Tailwind Utility Classes 和 Shadcn 语义化变量，避免硬编码色值。
4.  **数据请求**: 统一使用 `src/lib/api-client.ts` (基于 Ky) 进行网络请求。
5.  **代码提交**: 提交前会自动运行 Lint 和 TC 检查，请确保代码无报错。

## 📜 脚本说明

- `pnpm dev`: 启动开发服务器
- `pnpm build`: 构建生产环境代码
- `pnpm build-mock`: 构建带 Mock 数据的预览版本
- `pnpm lint`: 运行 Biome 代码检查
- `pnpm format`: 运行 Biome 格式化
- `pnpm typecheck`: 运行 TypeScript 类型检查
- `pnpm test`: 运行 Vitest 单元测试
- `pnpm routes:generate`: 手动生成路由树 (通常会自动监听)

---

**Happy Coding with AI! 🤖**
